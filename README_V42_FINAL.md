nightcomic v42 FINAL UI patch

This version is based on v40/v41 and contains a final, explicit override layer.
- Homepage: "همه ژانرها" is a real vertical one-column list, visually styled like the site's content sections.
- Explore: filter + 3.4 controls are compact on mobile.
- Bookmarks: old empty-library card is removed from markup/CSS behavior.
- Cache busting: style.css?v=42.
- VERSION file: 42.

IMPORTANT: deploy THIS ZIP / its contents to Railway. If Railway still shows the previous UI after deploying v42, it is serving an older deployment/branch rather than this package.
